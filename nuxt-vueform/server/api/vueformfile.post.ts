/**
 * Enpoint configured in the FileElement instance in /pages/index.vue
 * File data are being sent here when file is upladed but form not yet submitted
 */
export default defineEventHandler(() => {
  return 'OK (TODO implemement file handling)'
})
